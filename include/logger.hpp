#ifndef LOGGER_HPP
#define LOGGER_HPP

//Fake loggers; dont want to make this project to big with unnecessary stuff; use std::cerr 
//if u are interested i can upload my logger which uses boost log
#define LOG_TRACE(message) ;
#define LOG_INFO(message) ; 
#define LOG_WARN(message) ;
#define LOG_ERROR(message) ;

#endif 